# R8-1 pre-RC 社内共有用チェックリスト

## 位置づけ

このチェックリストは、UTR-S201 AI補助パッケージ v004 を社内で早期共有する前に確認するためのものです。

R8-1 は正式RCではありません。実機確認前の pre-RC 社内共有用確認段階として扱います。

## 共有前提

- 正式RCではない
- 実機確認未実施
- PDF原本は同梱しない
- API実装リポジトリではない
- 完成Hexを作成しない
- SUM計算済みコマンドを作成しない
- 実機送信用コードを作成しない
- 実機制御、永続設定変更、送信出力変更、周波数変更、FLASH書き込みは対象外

## 共有対象

- README.md
- docs/OPERATIONS.md
- artifacts/utr_s201_ai_v004/utr_s201_ai_v004_r7_5a_frontmatter_notes_cleanup_no_pdf.zip

## 共有前チェック

- [ ] mainを直接変更していない
- [ ] featureブランチで作業している
- [ ] README.mdの説明が現状と矛盾していない
- [ ] docs/OPERATIONS.mdの禁止事項と矛盾していない
- [ ] 成果物ZIPのファイル名がREADME.mdの記載と一致している
- [ ] 成果物ZIPのSHA256がREADME.mdの記載と一致している
- [ ] PDF原本を追加していない
- [ ] 完成Hexを追加していない
- [ ] SUM計算済みコマンドを追加していない
- [ ] 実機送信用コードを追加していない
- [ ] 実機確認済みと誤解される表現を追加していない
- [ ] 正式RCと誤解される表現を追加していない
- [ ] 周波数、送信出力、FLASH、アンテナ設定に影響する変更を追加していない

## Git確認

共有前に以下を確認します。

```text
git status --short
git diff
git diff --check
git log --oneline -5
git branch --show-current
```

## 共有時の注意書き

社内共有時は、以下の前提を明記します。

```text
R8-1 は pre-RC 社内共有用です。
正式RCではありません。
実機確認は未実施です。
PDF原本、完成Hex、SUM計算済みコマンド、実機送信用コードは含みません。
実機制御、永続設定変更、送信出力変更、周波数変更、FLASH書き込みは対象外です。
```

## RC化前に必要な確認

- 実機確認計画の作成
- 実機確認結果の記録
- 安全ルールと根拠資料の再確認
- 成果物ZIPの再生成要否確認
- PDF同梱有無の方針確認
- RC名称、版数、SHA256の確定
