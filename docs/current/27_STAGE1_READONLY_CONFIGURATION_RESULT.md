# Stage 1 読み取り専用設定確認結果

## 目的

Stage 1の設定値読み取り結果を整理します。

## 確認対象

Select、Inventory、ExpandSelect、アンテナ、出力、周波数、RFタグ通信関連、EPC(UII)関連、ポート、FLASH設定値などの読み取りを対象にします。

## 注意

読み取りコマンドでも、機種やROMにより非対応の場合があります。非対応は失敗ではなく、対象外または条件不足として記録します。
