# Data Handling Policy

## 目的

この文書は、UTR_RAG で扱うデータ、実機ログ、タグ固有値、完成Hex例の扱いを整理します。

公式PDFが一次情報です。このリポジトリは、公開可能な仕様整理とAI支援用の参照情報だけを扱います。

## 実機ログの扱い

実機ログは、原則としてこのリポジトリへコミットしません。

実機確認結果を共有する必要がある場合は、次の情報を削除またはマスクしてください。

- 実IPアドレス、ポート、端末名
- 顧客名、現場名、設置場所
- raw EPC / UII / TID
- Accessパスワード、Killパスワード
- 実運用タグを識別できる値

## raw EPC / UII / TID の扱い

実機ログ由来の raw EPC / UII / TID は公開しません。

説明上必要な場合は、公式PDF掲載例、または実タグを識別できない固定のダミー値だけを使います。実機ログ由来の値を一部だけ切り出して使う場合も、識別可能性が残らないようにマスクしてください。

## 完成Hexの扱い

公式PDFに掲載済みの完成Hex例は、仕様説明として記載できます。

ただし、実機ログ由来のタグ固有値、パスワード、現場固有設定を含むHexはそのまま載せません。SUM計算済みの送信用コマンド例を追加する場合は、目的、対象機種、ROM、接続方式、設定値、影響範囲、復旧方法、停止条件を確認してください。

## 生成物の扱い

RAGチャンクなどの生成物は、原則として手元で都度生成します。

`rag_chunks/` 配下の生成物はコミット対象外です。公開前に `git status --short` で未追跡ファイルが混ざっていないことを確認してください。

## 問題を見つけた場合

機密情報、実機ログ、認証情報、または公開すべきでないタグ固有値が混入している疑いがある場合は、公開Issueへ実値を貼らずに報告してください。

TODO: 正式な報告窓口を決めた後、`SECURITY.md` とあわせて更新してください。
